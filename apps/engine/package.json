{
  "name": "engine",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "cd v2 && cargo run",
    "dev:v1": "cd ../.. && bun --watch --env-file apps/server/.env apps/engine/v1/index.ts",
    "start": "cd v2 && cargo run --release",
    "start:v1": "cd ../.. && bun --env-file apps/server/.env apps/engine/v1/index.ts",
    "test": "cd v2 && cargo test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/config": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/events": "workspace:*",
    "@repo/kafka": "workspace:*",
    "decimal.js": "^10.6.0"
  }
}
